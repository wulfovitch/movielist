# this is a stub for a database.yml file in case you have lost it somewhere...
# rename to database.yml if you want to use this file or create a new one
development:
  adapter: mysql
  encoding: utf8
  database: movielist_development
  username: root
  password:
  host: localhost

# Warning: The database defined as 'test' will be erased and
# re-generated from your development database when you run 'rake'.
# Do not set this db to the same as development or production.
test:
  adapter: mysql
  encoding: utf8
  database: movielist_test
  username: root
  password:
  host: localhost

production:
  adapter: mysql
  encoding: utf8
  database: movielist_production
  username: root
  password: 
  host: localhost